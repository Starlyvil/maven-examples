pipeline {
	agent any
	stages {
		stage('Start') {
			steps {
				sh 'pwd'
			}
		}
		stage('deploy') {
			steps {
				sh '''
					cd java-multi-modules
					ls ~/.m2/ -al
					id
					mvn deploy
				'''
			}
		}
	}
}