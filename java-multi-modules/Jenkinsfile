pipeline {
	agent any
	stages {
		stage('Start') {
			steps {
				sh 'pwd'
			}
		}
		stage('deploy') {
			steps {
				sh '''
					cd java-multi-modules
					ls -al
					mvn package
					mvn deploy
				'''
			}
		}
	}
}